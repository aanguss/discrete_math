/** 
 * Chapter 5 - Probability
 * Section 5 - Probability Calculations in Hashing
 * Problem 5 - Page 322
 * Description -
In Problem 1, if there are 20 kinds of candy, how many children have to buy candy for the probability to be at least 1/2 that (at least) two children buy the same kind of candy?
 */

/** 
 * Chapter 5 - Probability
 * Section 5 - Probability Calculations in Hashing
 * Problem 5 - Page 322
 */
#include <stdlib.h>
#include <stdio.h>
#include <time.h>
#include <stdint.h>

int main(void)
{
   
    
    // start with seperation so results are easier to see
    printf("\n");
    

    // print results
    printf("\nnumber of 3 heads: %d, which gives P(H) = %f\n", num3Heads, (float)num3Heads/100);
    printf("number of at least 3 heads: %d, which gives P(H) = %f\n", num3PlusHeads, (float)num3PlusHeads/100);

    // some space
    printf("\n");
}